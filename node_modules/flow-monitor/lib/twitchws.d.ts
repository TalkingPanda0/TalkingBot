export declare const pubsubWSURL = "wss://pubsub-edge.twitch.tv/v1";
export type TwitchPayload = 'UNLISTEN' | 'LISTEN';
export declare function createNonce(length?: number): string;
export declare const payloads: {
    gameBroadcaster: (channelID: string, type?: TwitchPayload) => string;
    raid: (channelID: string, type?: TwitchPayload) => string;
    predictions: (channelID: string, type?: TwitchPayload) => string;
    videoPlayBack: (channelID: string, type?: TwitchPayload) => string;
};
