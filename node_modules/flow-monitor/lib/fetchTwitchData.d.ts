import { FetchError } from './types';
type FetchTwitchData = {
    userId: number;
    login: string;
    categoryId: number;
    categoryName: string;
    categoryImage: string;
    thumbnail: string;
    streamId: number;
    started_at: string;
    title: string;
    viewersCount: number;
    m3u8Url: string;
    error?: string;
    status?: number;
    message?: string;
};
export declare const fetchTwitchData: (channel: string, headers?: HeadersInit) => Promise<(FetchTwitchData & FetchError) | Partial<FetchTwitchData & FetchError>>;
export {};
