import { IChatYTMessage } from "../../types/Client";
import { IChatData } from "../../types/Types";
import { PoolChoices } from "./updateLiveChatPollAction";
export declare function createCommonUserResponse(messageData: Partial<IChatData>): IChatYTMessage;
export interface FormatPoolChoices {
    runs: {
        text: string;
    }[];
    voteRatio: number;
    votePercentage: string;
}
export declare function formatPoolChoices(choices: PoolChoices[]): FormatPoolChoices[];
